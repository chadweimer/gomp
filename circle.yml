machine:
  services:
    - docker
    
dependencies:
  pre:
    - docker build --rm=false -t cwmr/gomp:dev .

deployment:
  hub:
    branch: feature/default-user
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASSWORD
      - docker push cwmr/gomp:dev
