{
  "name": "gomp",
  "description": "Go Meal Planner: A web-based recipe book",
  "devDependencies": {
    "bower": "^1.7.9",
    "polymer-cli": "^1.0.0"
  },
  "scripts": {
    "postinstall": "cd static && ../node_modules/.bin/bower install --quiet && ../node_modules/.bin/polymer build --preset es6-unbundled"
  },
  "cacheDirectories": [
    "static/bower_components"
  ]
}
