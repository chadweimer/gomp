{
  "name": "gomp",
  "description": "Go Meal Planner: A web-based recipe book",
  "devDependencies": {
    "bower": "^1.7.9",
    "polymer-cli": "^0.16.0"
  },
  "scripts": {
    "postinstall": "cd static && bower install && polymer build"
  },
  "cacheDirectories": [
    "static/bower_components"
  ]
}
