{{template "shared/header" .}}
<section class="row">
    {{if .SearchQuery}}
    <p class="col s12 l12">
        Results for search '{{.SearchQuery}}': {{.ResultCount}}
    </p>
    {{end}}
    {{range .Recipes}}
    <article class="col s12 m6 l4">
        <div class="card small grey lighten-4 hoverable clickable" onclick="location.href = '/recipes/{{.ID}}';">
            {{if .Image}}
            <div class="card-image">
                <img src="{{.Image}}" class="darken-25">
                <span class="card-title">{{.Name}}</span>
            </div>
            {{end}}
            <div class="card-content">
                {{if not .Image}}
                <span class="card-title">{{.Name}}</span>
                {{end}}
                <p class="plain-text">{{.Description}}</p>
            </div>
        </div>
    </article>
    {{end}}
</section>
<section class="fixed-action-btn">
    <a href="/recipes/create" class="btn-floating btn-large waves-effect green">
        <i class="material-icons">add</i>
    </a>
</section>
{{template "shared/footer" .}}
