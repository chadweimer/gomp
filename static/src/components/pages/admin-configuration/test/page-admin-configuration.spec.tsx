import { PageAdminConfiguration } from '../page-admin-configuration';

describe('page-admin-configuration', () => {
  it('builds', () => {
    expect(new PageAdminConfiguration()).toBeTruthy();
  });
});
