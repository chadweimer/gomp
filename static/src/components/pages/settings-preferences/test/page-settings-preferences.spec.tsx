import { PageSettingsPreferences } from '../page-settings-preferences';

describe('page-settings-preferences', () => {
  it('builds', () => {
    expect(new PageSettingsPreferences()).toBeTruthy();
  });
});
